/** Common HTTP headers. */
export enum Header {
  ContentType = "content-type",
  Location = "location",
}

/** Common content types. */
export enum ContentType {
  Html = "text/html; charset=utf-8",
  Svg = "image/svg+xml",
}
